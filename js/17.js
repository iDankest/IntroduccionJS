//Codigo condicional

const  SaldoDisponible = 100;
const retirar = 50;

if(SaldoDisponible > retirar){// El if puede estar solo
    console.log("Retiro exitoso");
}else{//El else es opcional y se ejecuta cuando no se cumple la condicion
    console.log("Saldo insuficiente");
}