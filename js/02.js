// ejemplo de Const y su uso
const precio = 200;
precio = 300; // la variable en JS no se puede reasignar
console.log(precio);
//Esta variable no cambia su valor en ejecucion
// Tiene mayor seguridad que let y se recomienda usar const por defecto